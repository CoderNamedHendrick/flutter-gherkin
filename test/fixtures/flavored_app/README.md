# Flavored discovery fixture

A real Flutter-created Android project with local/preview Kotlin DSL flavors. Used
for nested-app discovery, flavor detection and scaffold idempotency. Native execution
is validated separately; flavor discovery alone is not a Patrol readiness claim.
