name: minimal_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  marionette_flutter: ^0.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  patrol: ^4.10.0
  patrol_mcp: ^0.2.1

flutter:
  uses-material-design: true
patrol:
  app_name: minimal_app
  ios:
    bundle_id: dev.gherkin.minimalApp
  android:
    package_name: dev.gherkin.minimal_app
  test_directory: gherkin/generated/patrol
